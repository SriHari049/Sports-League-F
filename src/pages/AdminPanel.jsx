import { Box, Typography } from '@mui/material';

const AdminPanel = () => {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h4">Admin Dashboard</Typography>
      {/* Add subcomponents for seating, scores, fixtures, player approvals */}
    </Box>
  );
};

export default AdminPanel;
